# Sports Rental — dependencias del proyecto
# Instalación:  python -m pip install -r requirements.txt
# Versiones fijadas a un mínimo probado; usar un entorno virtual (venv/conda).

# --- Generación del dataset (generate_dataset.py) ---
numpy>=2.2.0
pandas>=2.2.3
faker>=40.32.0

# --- Análisis y notebook (.ipynb en VSCode) ---
matplotlib>=3.10.0
seaborn>=0.13.2
plotly>=6.9.0          # treemaps y bubble charts interactivos
scipy>=1.14.1          # tests estadísticos e intervalos de confianza
statsmodels>=0.14.6    # regresión OLS / Logit e inferencia
duckdb>=1.5.5          # motor SQL local para las celdas %%sql (equivalente a Databricks SQL)
ipykernel>=6.29.5      # kernel Jupyter que usa la extensión de VSCode

# --- Opcional ---
scikit-learn>=1.6.0    # Isolation Forest para detección de anomalías multivariante (§7.7); el notebook lo omite si falta
# pyspark>=3.5           # solo para la demo de transformación distribuida (requiere Java)
